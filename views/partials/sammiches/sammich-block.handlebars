{{!-- Change sammiches to devoured  --}}

<li>
    {{name}}
    <button class="change-devour btn-small indigo darken-3" data-id="{{id}}" data-new="{{devoured}}">
        {{#if devoured}}ALREADY GONE!{{else}} EAT ME! {{/if}}
    </button>

    <button class="delete-sammich btn-small indigo darken-3" data-id="{{this.id}}">DELETE!</button>
</li>