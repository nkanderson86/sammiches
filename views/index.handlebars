{{!-- * Setup the `index.handlebars` to have the template that Handlebars can render onto. 

 * Create a button in `index.handlebars` that will submit the user input into the database.
--}}

<h1>Sammiches!</h1>

<h2>Add a Sammich</h2>
<form class="create-form">

    <div class="form-group">
        <label for="ca">Sammich Name:</label>
        <input type="text" id="sam" name="name">
    </div>

    <button type="submit" id="sam-create">Add Sammich</button>
</form>

<h2>Ready to Eat!</h2>

<ul>
    {{#each sammich}}
    {{#unless devoured}}
    {{sammich_name}}
    {{> sammiches/sammich-block devoured=false}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Sammiches from the past</h2>

<ul>
    {{#each sammich}}
    {{#if devoured}}
    {{sammich_name}}
    {{> sammiches/sammich-block devoured=true}}
    {{/if}}
    {{/each}}
</ul>